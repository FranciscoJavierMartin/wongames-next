import { styled } from '../../../styled-system/jsx';

export const Wrapper = styled('h2', {
  base: {
    color: 'primary',
  },
});

// export const Wrapper = ({ children, color }: any) => {
//   return <styled.h2 color={color}>{children}</styled.h2>;
// };
